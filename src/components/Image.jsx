import React from "react"

export default function Image({className}) {
    return (
        <div className={`${className} image-container`}>
            <img src="" alt="image" className="image-grid"/>
        </div>
    )
}
